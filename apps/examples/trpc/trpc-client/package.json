{
  "name": "@saga-soa/trpc-client",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup",
    "clean": "rm -rf dist",
    "check-types": "tsc --noEmit",
    "test": "tsx src/index.ts"
  },
  "dependencies": {
    "@saga-soa/trpc-types": "workspace:*"
  },
  "devDependencies": {
    "@saga/typescript-config": "workspace:*",
    "@types/node": "^24.0.12",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  }
} 